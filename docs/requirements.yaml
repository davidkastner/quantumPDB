name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip
  - matplotlib
  - numpy
  - pandas
  - seaborn
  - scikit-learn

    # Pip-only installs
  - pip:
    - sphinx==6.0.0
    - sphinx-autoapi
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
