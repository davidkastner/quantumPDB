name: docs
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Package depends
  - matplotlib
  - numpy
  - pandas
  - pytorch
  - seaborn
  - scikit-learn

    # Pip-only installs
  - pip:
    - sphinx==6.0.0
    - sphinx-autoapi
    - https://github.com/revitron/revitron-sphinx-theme/archive/master.zip
